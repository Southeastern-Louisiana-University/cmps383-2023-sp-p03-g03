import React from 'react';

export function DestinationsPage(): React.ReactElement {
    return (
        <div>
            <header> We couldn't find that page? It may still be in progress...</header>
        </div>
    );
}